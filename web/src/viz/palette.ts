/** Shared CSS-variable colour map for all six node types. */
export const HUE: Record<string, string> = {
  theme:     "var(--c-theme)",
  motif:     "var(--c-motif)",
  technique: "var(--c-technique)",
  effect:    "var(--c-effect)",
  character: "var(--c-character)",
  beat:      "var(--c-beat)",
};
