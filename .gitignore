# Python
__pycache__/
*.pyc
.venv/
venv/

# OS / editor
.DS_Store
*.swp

# Brainstorm visual companion(mockup 暫存,不版控)
.superpowers/

# Obsidian (stories/ 可當 vault;不版控其 app 設定)
.obsidian/

# 故事內容與其全部衍生物不進版控(使用者創作 + 可由腳本重生)。
# 含 source.md / analysis.* / feedback.* / viz.* / index.json。只有程式碼進版控。
stories/
# empty-state 測試時暫存真故事的備份,同屬使用者內容
stories_bak/

# Frontend build artifacts
web/node_modules/
web/dist/
# 前端動畫原型(視覺試驗 HTML,dev 直接開;不版控)
web/public/

# 內部開發文件(設計/現況/計畫)留本地,不進公開 repo
docs/

# 介紹簡報(HTML deck,本地/Windows 雙擊開;不版控)
slides/

# 本機 log 全收這:logger 的 app.log + dev.sh 的 server.log / web.log(不版控)
logs/

# 測試覆蓋率報告(本機產物)
.coverage
htmlcov/

# Playwright MCP 的截圖/快照(視覺驗證用暫存,不版控)
.playwright-mcp/
/*.png
